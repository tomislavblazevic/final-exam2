{
  "name": "todo-sync-server",
  "version": "1.0.0",
  "description": "Minimal Express server to sync todo tasks (protected by API key via x-api-key header)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
